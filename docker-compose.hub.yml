# Run the pre-built image from Docker Hub (mohitbhalothia007/ai-resume-analyzer)
services:
  app:
    image: mohitbhalothia007/ai-resume-analyzer:latest
    ports:
      - "8000:8000"
    restart: unless-stopped
